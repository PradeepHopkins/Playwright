🛠️ Configuration of a JavaScript Project
🧩 Setting up a JS project for the first time

1. Open terminal in your IDE (e.g., VS Code).

2. Navigate to the path where you want to create your project:
#   $ cd path/to/your/directory

3. Create a new folder for your project:   
#   $ mkdir project-name
#   $ cd project-name

4. Initialize npm (Node Package Manager):
#  $ npm init
   Press Enter for default options, or fill in details like project name, version, and description.

5. Run your project (if applicable):
#  $ node <fileName.js>   

💻 Setting up a JS project on your machine (after cloning or downloading)

1. Open terminal in the project’s root folder.

2. Install dependencies from the package.json file:
#  $ npm install

3. Start or run the project (depending on your configuration):
#  $ npm start

4. (Optional) If you’re using a development tool like nodemon, run:
#  $ npm run dev